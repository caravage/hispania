/* main.js — amorçage.
   Les fichiers sont chargés comme scripts classiques et non comme modules ES :
   le jeu s'ouvre alors par double-clic sur index.html, sans serveur local ni
   étape de compilation. L'ordre des balises <script> dans index.html fait la
   dépendance ; ce fichier vient en dernier. */

render();
